# gcc (build & run)
`gcc Main.c -o Main`  
`./Main`  

# clang (build & run)
`clang Main.c -o Main`  
`./Main`  

# requirements
**gcc**: `sudo apt install gcc`  
**clang**: `sudo apt install clang`  
