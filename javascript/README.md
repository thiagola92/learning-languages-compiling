# node (run)
`node Main.js`  

# deno (run)
`deno run Main.js`  

# deno (build & run)
`deno compile Main.js`  

# requirements
**node**: `sudo apt install -y nodejs`  
**deno**: `curl -fsSL https://deno.land/install.sh | sh`  
